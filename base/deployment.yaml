apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  replicas: 2
  template:
    spec:
      containers:
      - name: myapp
        image: quxia/myapp:tag  # Placeholder tag
        ports:
        - containerPort: 80
